import React from 'react';
interface TabsProps {
    tabs: {
        id: string;
        label: string;
    }[];
    activeTab: string;
    onTabChange: (tabId: string) => void;
}
declare const Tabs: React.FC<TabsProps>;
export default Tabs;
